{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Run current file on Pico",
            "type": "shell",
            "command": "${command:micropico.run}",
            "problemMatcher": []
        }
    ]
}